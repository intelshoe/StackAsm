
class Parse:
	def __init__(self, code, code_type):
    	self.code = code
    	self.code_type = code_type

    # parses code and renders blocks to screen
	def blocks(obj):
		# print source code file to string

		# parse code and render shapes

	# parses code and renders text explanation
	def text(obj)
